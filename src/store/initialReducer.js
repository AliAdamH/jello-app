import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const initialReducer = createSlice({
  name: 'initial',
  initialState,
  reducers: {},
});

export const {} = initialReducer.actions;

export default initialReducer.reducer;
